.main-content
  - if @user
    .page-title= @user.name
    .page-subtitle= link_to 'Portfolio', user_portfolio_path(@user, @user.portfolio)
    .page-subtitle= link_to "Edit Portfolio", edit_user_portfolio_path(current_user, @user.portfolio)